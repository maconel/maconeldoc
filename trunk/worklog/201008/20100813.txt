# 改bug 4368881。bug 4362971，bug 4362911，bug 4355271，bug 4318581一并被修改了。其中bug 4355271根本原因在底层，但上层修改后，不会再单里描述的情形下出现。另外OnCallback中虽然不再弹框了，但还是加了final级的log。
# 昨晚bug 4359041已经改好，但cyan合入时，改动被覆盖了。已重新修改。
# 改bug 4336351。最后一个现象，根据log，发现原因是调用底层的GetFolderId()失败了，并返回S_FALSE，而上层通过SUCCEEDED()来判断是否成功，导致即使失败也认为是成功。但这种情况本不应该失败的，这里失败本身就是问题，这个问题应是底层的，还不清楚原因。在看底层代码时发现，底层是用一个双重循环来实现，效率有点低，因此改成从从上层来获取，顺便实现了原本漏实现的CGroupPanelPage::GetGroupNode()。
