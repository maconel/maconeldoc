# gips码率问题。
    # 更新gips的库文件。上次发来的是错的，里边不支持isac。换了新的就支持了，可以和qq互通，而且码率也比较低，在20左右。
    # 为了配合新的gips库，音频能力id改回ptIsac = 100。
    # 之前有个问题，AndroidManifest.xml中如果不加上音频权限，会导致初始化gips失败，而且加上权限后，还是初始化gips失败，甚至整个系统的音频都不能启动了。换了新的gips库后，这个问题也解决了。
# 跟花屏问题。经过抓包，确认编码没有问题。怀疑是nosp模式的问题，经测，也不是。此问题继续解决中。
# 解决重复关闭打开摄像头异常的情况。原因是暂停回复视频时，LocalView中会多次开启关闭摄像头，导致摄像头打开异常。解决方法是在CameraFactory中对open, start, stop, close都加上保护。
