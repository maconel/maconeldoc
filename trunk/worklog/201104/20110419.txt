# 编写vp8性能测试模块，感觉用处不大，暂时不加入icu。
# cavanliu帮忙测试opengl是否可以在后台进行测试。结论是可以运行，但在后台跑的性能比前台差了很多。
# 讨论哪些参数需要测试。
    # 测试vp8性能，看与bogomips是否线性关系，如果是，则不需要在icu中实测vp8。
    # 测试gips性能。
    # 测试bogomips和是否支持硬opengl。
    # 测试第三方跑分程序。
# 修改原来的vp8单元测试程序，给pangzhang进行测试。
# 和showy讨论如何使用性能测试结果。
    # 性能测试可以用来得到初步数据。
    # 程序跑起来后，判断是否因策略限制而压制了fps，如果不是，则当前fps可以保存下来，供以后使用。
