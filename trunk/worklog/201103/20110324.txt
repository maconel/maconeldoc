# 音频与qq互通的问题。下午和phantom联调。
# 启动音频设备失败的问题。
    # 给audio_app和icu_app都加上反初始化保护，也就是加个标志，如果还没调过init()，直接调uninit()了，就通过这个标志判断出来，并直接返回，不做反初始化的操作。
        # 上述改动可以解决音频初始化失败的问题。
        # 上述改动可能可以解决进入视频界面又立刻返回的问题。
    # 音频加了些日志。
    # 张文定位到因为收到code=1，表示连接断开，因此本地也主动断开。code是由java层产生和解析，因此张文先查下java是否发出了这个命令，比如可能prepareview时1分钟倒计时的定时器忘关，导致产生code=1。
# 招聘相关。
    # 刘良说不来了。
    # 面试向昕。
# 培训，发现一个remember the milk的GTD网站，正在试用。
