# 安装android-ndk-r5b，测试native-plasma工程。
    # native-plasma需要2.3才能运行。这个在1.3版本里才用到，暂时不尝试了。
# ndk调用摄像头。
    # 尝试自己打开设备，在g7上有的设备没权限，有的设备打开后不能设置摄像头属性。此路不通。
    # android源码中底层是加载liboemcamera.so来进行摄像头操作的。尝试自己加载使用，结果是可以加载，但取不到需要的函数。而且zpad上没有这个so。此路不通。
# 关闭屏幕再打开，程序crash问题。
    # avdemo已经不会有问题，icuapp再次打开时，会有未捕获的异常，待查。
# 招聘相关。
    # 面试黄常州，没来。
    # 面试罗荣平，不通过。
