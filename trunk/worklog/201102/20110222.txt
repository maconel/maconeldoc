# 完成了hudson自动编译的全过程。
    # 解决了hudson从svn拉代码的问题，但现在只能用java直接运行war的方式跑，那个运行参数还不知道怎么用在tomcat中。
    # 解决了用ant编译时，jdk版本不对导致@Override报错的问题。
    # 完成了发送邮件的功能。
    # 完成了ndk编译的步骤。
# 研究skia，发现需要编译android才能得到静态库，而且它底层也是调用opengl，放弃。
